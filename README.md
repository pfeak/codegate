# codegate
一个轻量级的 Python 服务，提供邀请码/验证码生成、过期、验证服务API。既可独立运行，也可嵌入其他系统。 A lightweight Python service that provides APIs for generating, expiring, and verifying invitation codes/verification codes. It can run independently or be embedded in other systems.
